class Solution {
    public void reverseString(char[] s) {

    int j =1;
      for(int i=0;i<s.length/2;i++){
        char temp = s[s.length-j];
        s[s.length-j] = s[i];
        s[i] = temp;
        j++;
    }
  }

